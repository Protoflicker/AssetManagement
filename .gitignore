# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*.log

# dependencies (none required — this is a zero-build static site)
node_modules/

# Vercel
.vercel/

# Environment variables (keep credentials secret!)
.env
.env.local
.env*.local

# stray binaries (not part of this project)
*.exe
